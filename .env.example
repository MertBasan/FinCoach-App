# Postgres
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_DB=fincoach
POSTGRES_USER=fincoach
POSTGRES_PASSWORD=changeme_local_only

# App
SECRET_KEY=replace_with_long_random_string_in_production
SESSION_COOKIE_NAME=fincoach_session
ENVIRONMENT=development

# AI (not used in foundation, placeholders for later)
ANTHROPIC_API_KEY=
